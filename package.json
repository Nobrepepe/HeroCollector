{
  "name": "hero-collector",
  "version": "1.0.0",
  "description": "Hero Collection Progression Game — MVP (menu-first, single-player)",
  "main": "electron/main.js",
  "type": "module",
  "scripts": {
    "start": "electron .",
    "generate": "node scripts/generate-content.mjs",
    "test": "node --test \"tests/**/*.test.mjs\""
  },
  "devDependencies": {
    "electron": "^33.0.0"
  }
}
